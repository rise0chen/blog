version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: git submodule update --init --recursive
      - run: yarn install
      - run: npm run deploy
